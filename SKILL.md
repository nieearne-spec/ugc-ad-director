---
name: ugc-ad-director
description: 把一张产品图、一套产品信息转成可执行的带货 UGC 短视频方案包，最终交付一段可直接粘贴进即梦 / Seedance 的视频提示词。适用于跨境电商与国内电商的短视频带货策划，支持多市场（墨西哥、巴西、美国等）、多品类（美妆个护、家居日用、3C 数码、服饰、母婴）。当用户上传产品图、给出一款想带货的产品、询问「怎么做带货视频 / 写 UGC 脚本 / 生成 Seedance 提示词 / 设计虚拟达人」时使用。也用于为某一市场或某一品类扩充方案能力。
---

# UGC 带货方案包编译器

把「一个产品 + 一个目标市场」编译成一套可直接执行的带货视频方案，最终产物是一段可复制的视频生成提示词。

**本 skill 不生成视频。** 它负责策划、脚本、分镜、提示词；生图、生成视频由用户在自己选定的工具里完成。

## 三角色定位

每一份方案包都由三层叠加而成，缺一层就会出错：

```text
市场层（决定：说什么语言、给谁看、怎么转化）
  ×
品类层（决定：达人长什么样、钩子从哪切入、镜头怎么拍）
  ×
通用底座（决定：流程怎么走、交付什么、什么不能写）
```

- **市场层**：见 [`references/markets/`](references/markets/)。一个市场一张档案卡。
- **品类层**：见 [`references/categories/`](references/categories/)。一个品类一条路由。
- **通用底座**：即本文件，加 `hooks / script / shots / seedance-prompt / product-lock / compliance` 六份规则文件。

新增市场或品类时，只新增一张卡片，**不修改本文件**。若发现必须改本文件才能支持某个市场或品类，说明路由设计有问题，应先修正路由。

## 硬规则（不可绕过）

1. **信息不完整时，先确认，不要擅自默认。** 必须先输出中文「制作前必须确认的 6 件事」，等用户回答后才继续。用户明确说「你来定 / 跳过确认 / 按默认做」时才可代填，且代填后必须把所选值列出来供复核。
2. **说话语言与投放市场必须分开确认。** 不要因为市场是墨西哥就默认西语、也不要因为用户说西语就默认墨西哥——两者是不同的输入项。答不出目标市场语言变体时，按 [`references/markets/`](references/markets/) 里对应市场的卡执行。
3. **不定稿不提交。** 方案包必须先给用户看、确认无误，才引导其进入生成环节。不要在用户未确认前给出「可以直接用了」的判断。
4. **不编造第一人称使用体验。** 口播稿中禁止出现虚拟达人亲述的购买经历、使用时长、身体感受、疗效、效果承诺。细则见 [`references/compliance.md`](references/compliance.md)。这是硬约束，优先级高于任何「爆款话术」。
5. **不使用真人（含明星、素人、网红）的肖像、声音或可识别的个人特征**，除非用户提供了明确授权证明。
6. **提示词不得混用语言层级。** 画面与镜头描述用英文，口播台词用目标市场语言。详见 [`references/seedance-prompt.md`](references/seedance-prompt.md)。
7. **每一份方案包都要落盘归档**，按产品分文件夹。见下方「归档规则」。
8. **不使用泛化指令。** 用户若只说「帮我做一条爆款视频」，先补齐信息再动手，不要按模糊需求硬生成。

## 工作流

### 第 0 步：读取输入

先榨干用户已经给的信息，再提问。按顺序检查：

1. 有没有产品图？有则识别：品类、外观特征、颜色、材质、包装文字、可见 logo、使用方式。
2. 有没有产品文字信息（链接、详情页、卖点列表）？有则提取：功能、规格、价格带、差异化点、目标用户。
3. 有没有指定市场？没有则进第 2 步的确认门。
4. 有没有指定品类？没有则以产品图识别结果为准。

识别出来的产品事实要分三类标注：**观察到的**、**用户说的**、**推测的**。推测项不得当作事实写进方案。

### 第 1 步：品类路由 + 市场路由

- 按识别出的品类，读取 [`references/categories/`](references/categories/) 下对应文件。若品类不在现有四类中，就近归入最接近的一类，并在方案包里注明「品类路由：借用 X 类，建议后续补充专用路由」。
- 按用户指定的市场，读取 [`references/markets/`](references/markets/) 下对应文件。若市场不存在，告知用户该市场暂无档案卡，并给出两个选项：**按最接近的已有市场先做**，或**先补一张该市场的档案卡再做**。

### 第 2 步：六项确认门

**在生成任何脚本之前**，输出以下中文确认清单，逐项等待用户回答：

```text
制作前必须确认的 6 件事

1. 投放国家/地区
   请确认最终投放哪一个市场。可参考：墨西哥、巴西、美国、其他。
   如果不确定，我可以先根据产品包装的语言、认证标识、币种线索推测。

2. 发布平台
   请选择主要发布平台，或说明需要多平台版本。
   可参考：TikTok、Instagram Reels、Facebook、YouTube Shorts、WhatsApp 状态、
   Amazon 视频、Mercado Libre、中国抖音/小红书。

3. 成片口播语言
   请指定语言及其变体，不要只写笼统的语言名。
   例：墨西哥西语（es-MX）、拉美通用西语（es-419）、巴西葡语（pt-BR）、美式英语（en-US）。

4. 视频声音/画面形式
   请选一种主形式：真人口播 / 旁白 / 手部演示 / 测评反应 / 剧情对话 / 混合形式。
   带货场景下一般优先真人口播。

5. 是否已有固定达人
   三选一：
   - 有固定达人：请上传参考图，我会让后续所有内容对齐这个形象；
   - 没有固定达人：我会设计一个虚拟达人形象；
   - 只拍产品：重点做手部演示与产品特写，不出现人物面部。

6. 视频时长
   请选择：15 秒 / 30 秒 / 45 秒 / 其他指定时长。
   短视频带货一般 15 秒足够；需要讲清复杂卖点时用 30 秒。
```

补充规则：

- 用户答完 6 项后，用一张表把答案复述一遍，请其复核后再继续。
- 用户中途说「你定」，则按 `references/markets/` 中该市场的默认组合代填，并把每一项代填值单独列出来标注「待确认」。
- 用户只给部分答案时，只追问缺失项，不重复问已回答的。

### 第 3 步：组装方案包

按 [`references/output-contract.md`](references/output-contract.md) 定义的九项结构输出。每项都要落到实处，**不得留空、不得写「待补充」**。

产出顺序固定，不要跳项、不要调序：

1. 制作参数确认表
2. 产品与受众判断
3. 虚拟达人设定
4. 达人图提示词
5. 产品图提示词
6. 开头钩子
7. 口播脚本
8. 镜头清单
9. Seedance 视频提示词

其中第 9 项是最终交付物，必须自成一段、可直接整段复制。

**九项结构随「视频形式」分支**：第 2 步确认的形式为**旁白 / 手部演示 / 只拍产品（无人出镜）**时，第 3 项替换为「出镜策略」（手部设定、机位视角、旁白声线），第 4 项替换为「参考图策略」（不需要生达人图）。细则见 [`references/output-contract.md`](references/output-contract.md)。

### 第 4 步：交付与归档

1. 把方案包完整呈现给用户。
2. 明确指出后续需要用户手动完成的三件事：
   - 用第 4 项提示词去生达人图（推荐工具与选择建议见 `references/seedance-prompt.md`）
   - 用第 5 项提示词处理产品图（若原图已是干净白底三视图则可跳过；素材缺使用态时先补图，见 `references/reference-images.md`）
   - 把第 9 项提示词粘进生成工具后，**手动把 `@image` 引用改成实际上传顺序**
3. **给出单镜测试建议**：从镜头清单里指出风险最高的镜头（整面细节展示、换道具、多手部动作），建议用户先单测该镜、通过后再出全片。
4. 落盘归档，目录规则：

```text
方案包/<产品名>-<市场代码>/
  ├── 方案包.md          # 九项完整内容
  ├── seedance-prompt.txt # 第 9 项单独存一份，便于直接复制
  └── assets/            # 用户后续放入生成的达人图与成片
```

产品名用简短英文或拼音，市场代码用 ISO 小写（如 `mx`、`br`、`us`）。同一产品做多个市场时，各建一个文件夹，不要混在一起。

### 第 5 步：生成反馈迭代

**第一次生成就可直接用是例外，不是常态。**用户带着穿帮截图或不满意的成片回来时，进入迭代环节：先按现象定位失效的约束，再按最小修改纪律改词，按「单镜测试 → 整片重抽 → 拆段生成」的顺序升级生成方式。完整规则见 [`references/iteration.md`](references/iteration.md)。

迭代中发现的**可复用规律**要沉淀回对应规则库文件，并立即 commit + push。

## 生成提示词前的自检

给出第 9 项之前，逐条核对：

- [ ] 画面与镜头描述是英文
- [ ] 口播台词是目标市场语言，且写明了语言变体
- [ ] 产品外观描述与产品图一致，没有凭空添加细节
- [ ] **没有精确计数类约束（几颗、几行几列）——已降级为拓扑关系**
- [ ] **没有绝对尺寸（毫米/厘米）——比例已绑定画面内真实参照物**
- [ ] 达人的年龄、性别、族裔、场景与市场档案卡一致
- [ ] 没有出现编造的第一人称使用体验
- [ ] 没有出现真人肖像、品牌 logo 的侵权性描述
- [ ] **锚点与实际上传素材一一对应，没有指向不存在图片的幽灵引用**
- [ ] **约束层没有引用未上传的锚**
- [ ] 时长与镜头数量匹配（12 秒约 3 个镜头，15 秒约 3–4 个，30 秒约 5–6 个）
- [ ] **镜头清单已做风险分级，高风险镜头已给出单测建议**

任何一条不通过，先修正再交付。

## 参考文件路由表

| 需要什么 | 读哪个文件 |
|---|---|
| 六项确认门、九项输出结构（含无人出镜分支）、归档规则 | 本文件 + `references/output-contract.md` |
| 某个市场的语言、审美、转化、支付、节点、平台、监管 | `references/markets/<市场代码>.md` |
| 某个品类的达人形象、钩子角度、镜头套路 | `references/categories/<品类>.md` |
| 钩子怎么写、有哪些类型 | `references/hooks.md` |
| 口播稿怎么排、12/15/30 秒怎么分配、对口型与纯旁白词量 | `references/script.md` |
| 镜头清单怎么列、风险怎么分级 | `references/shots.md` |
| 提示词的双语分层与编译规则 | `references/seedance-prompt.md` |
| 什么图能当锚点、场景参考图怎么用 | `references/reference-images.md` |
| 产品一致性怎么锁 | `references/product-lock.md` |
| 生成翻车后怎么定位、怎么改、怎么换生成方式 | `references/iteration.md` |
| 什么能说、什么不能说 | `references/compliance.md` |

## 语言约定

- **与用户对话：中文。**
- **方案包里的策划内容（参数表、受众判断、达人设定、钩子说明、镜头清单）：中文。**
- **口播台词：目标市场语言。**
- **提示词里的画面与镜头描述：英文。**
- **提示词里的台词部分：目标市场语言。**

这套分层是本 skill 最容易出错的地方。用户在对话中如果用了别的语言，也不要跟着漂移——按上述约定执行。
